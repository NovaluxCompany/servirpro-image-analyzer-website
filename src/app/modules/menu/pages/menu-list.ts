import { Component, signal } from '@angular/core';

@Component({
  selector: 'app-menu',
  imports: [],
  templateUrl: './menu-list.html',
  styles: ``,
})
export class Menu {
    showFormModal = signal(false);

    
}
